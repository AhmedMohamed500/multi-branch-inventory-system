# Setup

1. ثبّت Node.js 20 أو أحدث.
2. شغّل `npm install` داخل مجلد المشروع.
3. شغّل `npm run dev`.
4. افتح `http://localhost:3000` وسجّل الدخول بالحساب التجريبي الموجود في README.

لا يحتاج المشروع إلى ملف `.env` حاليًا. المتغير `NEXT_PUBLIC_API_BASE_URL` محجوز للربط المستقبلي.
